select * FROM systab

where table_name = 'Recp_Product_Type'

select *

FROM sysdbspace

CREATE DBSPACE SBC_FINTELLIX_DBSPC

USING FILE SBC_FINTELLIX_DBSPC

'SBC_FINTELLIX_DBSPC_01.iq'

SIZE 1024 MB

RESERVE 200;

select * from sysuser where user_name like 'SBC%'

GRANT CREATE ON SBC_FINTELLIX_DBSPC TO PUBLIC;

set option SBC_FINTELLIX_DH.DEFAULT_DBSPACE = 'SBC_FINTELLIX_DBSPC'

________________________________

Shwetha Bhat 4:48 PM
select distinct ssd.dbspace_id, ssd.dbspace_name, sst.table_id, sst.table_name

from systab sst

inner join sysdbspace ssd

    on sst.dbspace_id = ssd.dbspace_id

where ssd.dbspace_name like 'DSPDH%'

select  ssd.dbspace_id, ssd.dbspace_name, sst.table_id, sst.table_name

from systab sst

inner join sysdbspace ssd

    on sst.dbspace_id = ssd.dbspace_id

where ssd.dbspace_name like 'TPB%'

